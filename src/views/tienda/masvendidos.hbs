<td><a href="agregar" class="btn btn-primary">Volver</a></td>
<div class="container" p-4>
<div class="col-md-8">
    <div class="card-body">
        <h1>Productos</h1>
            <table class="table table-dark">
                <thead>
                    <tr class="table-active">
                        <th scope="col">id</th>
                        <th scope="col">Nombre</th>
                        <th scope="col">Mas vendidos</th>     
                    </tr>
                </thead>
                    <tbody>
                        {{#each product}}
                        <tr>
                        <th scope="row">{{id}}</th>
                        <td>{{nombre}}</td>
                        <td>{{masvendidos}}</td>
                        {{/each}}
                    </tbody>
            </table>
    </div>
</div>
</div>